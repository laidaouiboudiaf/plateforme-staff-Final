{% extends 'frontend/base.html.twig' %}

{% block title %}Client{% endblock %}

{% block body %}
    <h1>Client</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>IdClient</th>
                <td>{{ client.idClient }}</td>
            </tr>
            <tr>
                <th>NomEntreprise</th>
                <td>{{ client.nomEntreprise }}</td>
            </tr>
            <tr>
                <th>Siret</th>
                <td>{{ client.siret }}</td>
            </tr>
            <tr>
                <th>Adresse_facturation</th>
                <td>{{ client.adresseFacturation }}</td>
            </tr>
        </tbody>
    </table>

    <a class="btn btn-primary" href="{{ path('admin_client_index') }}">back to list</a>

    <a class="btn btn-warning" href="{{ path('admin_client_edit', {'idClient': client.idClient}) }}">Editer</a>

    {{ include('backoffice/admin_client/_delete_form.html.twig') }}
{% endblock %}
