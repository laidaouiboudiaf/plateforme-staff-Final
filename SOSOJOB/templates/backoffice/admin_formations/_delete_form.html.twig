<form method="post" action="{{ path('admin_formations_delete', {'idFormations': infos_formation.idFormations}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ infos_formation.idFormations) }}">
    <button class="btn">Delete</button>
</form>
