<form method="post" action="{{ path('admin_missions_delete', {'idMissions': mission.idMissions}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ mission.idMissions) }}">
    <button class="btn">Delete</button>
</form>
