<form method="post" action="{{ path('admin_bancaire_clt_delete', {'idBancairesC': infos_bancaires_c.idBancairesC}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ infos_bancaires_c.idBancairesC) }}">
    <button class="btn btn-danger">Delete</button>
</form>
