<form method="post" action="{{ path('admin_bancaire_fr_delete', {'idBancairesF': infos_bancaires_f.idBancairesF}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ infos_bancaires_f.idBancairesF) }}">
    <button class="btn">Delete</button>
</form>
