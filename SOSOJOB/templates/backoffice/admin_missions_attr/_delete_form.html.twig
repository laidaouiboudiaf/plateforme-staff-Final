<form method="post" action="{{ path('admin_missions_attr_delete', {'id': missions_attr.idMissionsAttr}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ missions_attr.idMissionsAttr) }}">
    <button class="btn">Delete</button>
</form>
