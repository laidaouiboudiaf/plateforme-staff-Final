{% extends 'frontend/base.html.twig' %}

{% block title %}Voir un type de Mission{% endblock %}

{% block body %}
    <h1>TypeDemission</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ type_demission.id }}</td>
            </tr>
            <tr>
                <th>NomTypeMission</th>
                <td>{{ type_demission.nomTypeMission }}</td>
            </tr>
        </tbody>
    </table>

    <a class="btn btn-primary" href="{{ path('admin_type_mission_index') }}">back to list</a>
<br>
    <a class="btn btn-warning" href="{{ path('admin_type_mission_edit', {'id': type_demission.id}) }}">edit</a>

    {{ include('backoffice/admin_type_mission/_delete_form.html.twig') }}
{% endblock %}
