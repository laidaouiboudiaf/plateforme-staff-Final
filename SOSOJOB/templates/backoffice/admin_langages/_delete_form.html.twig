<form method="post" action="{{ path('admin_langages_delete', {'idLangages': infos_langage.idLangages}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ infos_langage.idLangages) }}">
    <button class="btn">Delete</button>
</form>
